// Genkit configuration - simplified for build compatibility
import { defineFlow } from '@genkit-ai/flow'

// Basic Genkit setup without complex configuration
// This file is used for development and can be configured separately
export default {
  // Genkit flows are defined in individual files
  // This is a placeholder configuration
}
